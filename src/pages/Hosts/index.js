import React, { Fragment } from 'react';
import PageTitle from '../../layout-components/PageTitle';

export default function Hosts() {
  return (
    <Fragment>
      <PageTitle
        titleHeading="Hosts"
        titleDescription="This is a hosts page example built using this template."
      />
    </Fragment>
  );
}
